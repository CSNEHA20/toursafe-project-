apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: toursafe

resources:
  - configmaps-secrets.yaml
  - api-deployment.yaml
  - worker-deployment.yaml
  - ml-deployment.yaml
  - hpa.yaml
  - network-policies.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/part-of: toursafe-platform
